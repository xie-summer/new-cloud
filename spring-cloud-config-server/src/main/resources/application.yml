server:
  port: 4001

spring:
  application:
    name: cloud-config-server
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          search-paths: config-repo
          uri: https://github.com/xie-summer/new-cloud-config/
          username: xie-summer                                          # git仓库的账号
          password: www.5201314.qq.n
---
spring:
  profiles: dev
eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    serviceUrl:
      defaultZone: http://user:user@eureka-server:8761/eureka

---
spring:
  profiles: prd
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://user:user@eureka-server:8761/eureka
